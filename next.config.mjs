
/** @type {import('next').NextConfig} */
const nextConfig = {
      // Specify the base directory for Next.js
  pageExtensions: ['js', 'jsx'], // Ensure both .js and .jsx files work as pages
  sassOptions: {
  },
};

export default nextConfig;
